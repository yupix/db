[package]
name = "setup"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "setup"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive", "color"] }
dialoguer = { version = "0.11", features = ["fuzzy-select"] }
console = "0.15"
rand = "0.8"
